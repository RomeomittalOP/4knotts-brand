# Copy to ".env" (frontend root). Vite only exposes vars prefixed with VITE_.

# Your backend URL (Render). Local dev: http://localhost:5000
VITE_API_URL=https://fourknotts-backend.onrender.com

# Optional — backend already returns the public key, so this is just a fallback.
VITE_RAZORPAY_KEY_ID=
